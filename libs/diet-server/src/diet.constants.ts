import * as t from '@/diet-server/diet.types'

export const ITEM_TYPES: Record<string, t.ItemType> = {
  MEAL: 'meal',
  PRODUCT: 'product'
}
export const GENDER = {
  MALE: 'male',
  FEMALE: 'female',
}

